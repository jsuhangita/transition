import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  toggle__container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
});
